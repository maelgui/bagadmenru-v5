apiVersion: apps/v1
kind: Deployment
metadata:
  name: mailpit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mailpit
  template:
    metadata:
      labels:
        app: mailpit
    spec:
      containers:
        - name: mailpit
          image: axllent/mailpit:latest
          # Every captured message is also forwarded to a single real mailbox
          # (beta-sandbox@bagadmenru.bzh) via OVH SMTP, in addition to being
          # readable in the web UI (exposed at /_mail behind the site session,
          # see mailpit-ingress.yaml). Mailpit captures everything as usual;
          # forwarding is an additional copy. Configured entirely through
          # MP_* env vars. OVH port 465 is implicit SSL/TLS, so
          # TLS=true and STARTTLS=false. The password comes from the
          # mailpit-secrets sealed secret so it never lands in the manifest.
          env:
            # Serve the UI under /_mail so Mailpit emits its own asset, API and
            # WebSocket URLs with that prefix. Same webroot as the local compose
            # stack (docker-compose.yml) and the vite proxy, so the E2E Mailpit
            # URL shape is identical everywhere. Matches the Ingress path and
            # avoids rewriting responses (no stripPrefix needed).
            - name: MP_WEBROOT
              value: _mail
            - name: MP_SMTP_FORWARD_HOST
              value: ssl0.ovh.net
            - name: MP_SMTP_FORWARD_PORT
              value: "465"
            - name: MP_SMTP_FORWARD_TLS
              value: "true"
            - name: MP_SMTP_FORWARD_STARTTLS
              value: "false"
            - name: MP_SMTP_FORWARD_AUTH
              value: login
            - name: MP_SMTP_FORWARD_USERNAME
              value: beta-sandbox@bagadmenru.bzh
            - name: MP_SMTP_FORWARD_TO
              value: beta-sandbox@bagadmenru.bzh
            - name: MP_SMTP_FORWARD_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mailpit-secrets
                  key: SMTP_FORWARD_PASSWORD
          ports:
            - containerPort: 1025
              name: smtp
            - containerPort: 8025
              name: http
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: mailpit
spec:
  selector:
    app: mailpit
  ports:
    - name: smtp
      port: 1025
      targetPort: 1025
    - name: http
      port: 8025
      targetPort: 8025
