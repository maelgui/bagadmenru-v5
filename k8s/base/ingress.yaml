# K3s uses Traefik by default. This uses standard Ingress resources
# which Traefik handles natively.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bagadmenru-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: "default-redirect-https@kubernetescrd,default-hsts@kubernetescrd"
spec:
  tls:
    - hosts:
        - DOMAIN_PLACEHOLDER
      secretName: bagadmenru-tls
  rules:
    - host: DOMAIN_PLACEHOLDER
      http:
        paths:
          # Backend API on the same origin (no CORS). Backend serves /api/v1.
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
