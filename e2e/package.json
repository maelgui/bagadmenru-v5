{
  "name": "bagadmenru-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:local": "BASE_URL=http://localhost:5173 MAILPIT_URL=http://localhost:5173/_mail playwright test",
    "test:ci": "BASE_URL=https://beta.bagadmenru.bzh MAILPIT_URL=http://localhost:8025/_mail playwright test",
    "test:smoke": "playwright test --config=playwright.smoke.config.ts",
    "test:smoke:beta": "BASE_URL=https://beta.bagadmenru.bzh playwright test --config=playwright.smoke.config.ts",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^25.6.2"
  }
}
